rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Test mode: open access until 2026-06-20. Tighten before production.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 6, 20);
    }
  }
}
